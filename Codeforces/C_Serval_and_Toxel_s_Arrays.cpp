#include <bits/stdc++.h>
//#define int long long
using namespace std;

void solve(){
int N, M; cin >> N >> M; vector<int> A(N); for(auto &a : A) cin >> a;
vector<vector<int>> B(M);
while(M--){
    int 
}
}

int32_t main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}